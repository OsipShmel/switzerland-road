FROM alpine/socat:latest

ENV UPSTREAM_HOST=host.docker.internal
ENV UPSTREAM_PORT=11435

EXPOSE 11435
ENTRYPOINT []
CMD sh -c "socat TCP-LISTEN:11434,fork,reuseaddr TCP:${UPSTREAM_HOST}:${UPSTREAM_PORT}"

