[project]
name = "flag-injector"
version = "0.1.0"
description = "CTF flag injector for pentest agents"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
flag-injector = "tools.keys.key:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.uv.sources]