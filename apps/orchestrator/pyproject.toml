[project]
name = "orchestrator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "data-enricher",
    "scoring-agent",
]

[tool.uv.sources]
data-enricher = { workspace = true }
scoring-agent = { workspace = true }

[project.scripts]
orchestrator = "orchestrator:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
