[project]
name = "switzerland-road"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "docker>=7.1.0",
    "httpx>=0.28.1",
    "pydantic>=2.13",
]

[tool.uv.workspace]
members = [
    "apps/*",
    "packages/*",
]
