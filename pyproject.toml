[project]
name = "switzerland-road"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13",
]

[tool.uv.workspace]
members = [
    "apps/pentest_agent",
    "apps/data_enricher",
    "apps/orchestrator",
]
